# /skill — the Mantle AI Agent Skill (Phase 3, the bonus)

Open Anthropic `SKILL.md` folder format (verified spec: agentskills.io/specification). This skill
wraps the MantleFlow MCP server so any skills-aware agent can map RWA distribution on Mantle. Earns
the Research Challenge's "AI Agent Skills" bonus. The `SKILL.md` (frontmatter `name` + `description`,
plus instructions and `scripts/`) is authored in Phase 3.
