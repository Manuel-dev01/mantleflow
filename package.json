{
  "name": "mantleflow",
  "version": "0.0.0",
  "private": true,
  "description": "AI research agent that maps the distribution (not issuance) of tokenized/RWA assets on Mantle.",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
